#include <iostream>
#include <stack>
#include "stackArray.cpp"
// #include "stackLL.cpp"

using namespace std;

int main(){

    // //Implementation of Stack using Linked List
    // Stack S;

    // S.push(2);
    // S.push(9);
    // S.push(12);
    // S.push(0);

    // //2 9 12 0(top)
    // cout<<"Size of stack is: "<<S.sizeOfStack()<<endl;
    // cout<<S.peek()->data<<endl;
    // S.pop();
    // cout<<S.peek()->data<<endl;
    // S.pop();
    // if(S.isEmpty()){
    //     cout<<"Stack is Empty"<<endl;
    // }
    // else cout<<"Stack is not Empty"<<endl;

    // cout<<S.peek()->data<<endl;
    // S.push(4);    
    // cout<<S.peek()->data<<endl;

    // cout<<"Size of stack is: "<<S.sizeOfStack();

    // //Implementation of Stack using array
    // Stack S(10);

    // //2 1 0 6(top)
    // S.push(2);
    // S.push(1);
    // S.push(0);
    // S.push(6);

    // cout<<"Size of stack is: "<<S.sizeOfStack()<<endl;
    // cout<<"Top element in stack: "<<S.peek()<<endl;

    // S.pop();
    // S.pop();

    // cout<<"Size of stack is: "<<S.sizeOfStack()<<endl;
    // cout<<"Top element in stack: "<<S.peek()<<endl;

    // if(S.isEmpty()) cout<<"Stack is empty"<<endl;
    // else cout<<"Stack is not empty"<<endl;

    // S.pop();
    // S.pop();

    // if(S.isEmpty()) cout<<"Stack is empty"<<endl;
    // else cout<<"Stack is not empty"<<endl;

    // //Stack in STL
    // //creation of stack (stack<data type>)
    // stack <int> S;

    // //to insert/push an element in stack
    // S.push(4);
    // S.push(3);
    // S.push(9);

    // //to remove/pop an element from stack
    // cout<<"Top element of stack: "<<S.top()<<endl;
    // S.pop(); //pop the top element
    // cout<<"Top element of stack: "<<S.top()<<endl;

    // //to check if stack is empty or not
    // cout<<endl<<S.empty();

    // //to check the size of stack
    // cout<<endl<<S.size();
}